[project]
name = "dropbox-governed-execution-relay"
version = "1.0.0"
description = "Governed multi-call immutable execution relay from cloud transport to MOH with CHM completion publication"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false
